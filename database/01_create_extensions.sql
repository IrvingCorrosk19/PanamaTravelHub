-- Extensión para UUID
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Extensión para JSONB (ya viene por defecto, pero la incluimos por claridad)
-- CREATE EXTENSION IF NOT EXISTS "pg_trgm"; -- Para búsquedas de texto si es necesario
